# YOLO26 实验五六瘦身说明

本目录是 `exp5_6_yolo26` 的“轻数据、全图片证据版”：不再保留大体量原始数据集和训练集图片，但保留全部生成的数据分析图片、训练/验证/预测输出图、真实 Ubuntu 截图和原始报告提取截图，方便报告排版和答辩查证。

## 完整备份

瘦身前已经完整备份到：

`~\Desktop\作品集\嵌入式系统课程设计\嵌入式系统课程设计_2026春\exp5_6_yolo26_完整备份_20260523_185124`

备份包含原始数据集、训练集、验证集、全量预测结果、全量截图和所有中间文件。

## 瘦身保留内容

- `configs/`：YOLO 数据集和训练配置。
- `scripts/`：数据转换、数据划分、配置生成等脚本。
- `docs/`、`README.md`、`YOLO26实验五六项目详解.md`：实验说明文档。
- `runs/yolo26/train/weights/best.pt`：训练得到的 PyTorch 最优权重。
- `runs/yolo26/train/weights/best.onnx`：导出的 ONNX 模型。
- `runs/`：完整保留训练、验证、预测输出结果，包括训练曲线、batch 图、混淆矩阵、预测图和权重文件。
- `screenshots/`：完整保留生成的数据分析图片、整理后的截图、真实 Ubuntu 终端截图、Netron 界面截图和原始实验报告图片提取结果。

## 瘦身移除内容

- `external/`：完整 VisDrone 数据集压缩包和解压数据。
- `data_raw/`：原始图片和标签的中间整理结果。
- `my_data/`：YOLO 训练集和验证集全量图片/标签。
- 大体积初始权重 `yolo26m.pt` 和训练末轮权重 `last.pt`。

如需复查完整训练数据或重新训练，请从完整备份目录恢复。

## 当前瘦身目录规模

当前目录保留全部图片证据后约 275 MB，主要文件分布如下：

| 目录 | 文件数量 | 说明 |
|---|---:|---|
| `configs/` | 4 | 数据集配置与本地路径配置 |
| `scripts/` | 7 | 数据准备、划分、导出辅助脚本 |
| `runs/` | 116 | 训练、验证、预测输出和模型权重 |
| `screenshots/` | 237 | 全量生成图片、真实截图、原始报告提取图 |
| `detect_test/` | 7 | 少量测试输入样例 |
| `docs/`、`assets/` | 3 | 说明材料和示意资产 |

## 推荐报告插图

正文不需要把全量图片都插入，但本目录已经保留全部图片证据。报告正文推荐只选以下类型：

1. Ubuntu 训练命令或 epoch 输出截图 1-2 张。
2. 训练指标曲线 1 张。
3. 训练中期 batch 或数据增强图 1 张。
4. 验证 PR 曲线或混淆矩阵 1 张。
5. ONNX/Netron 截图 1 张。

这样可以证明实验五、实验六确实完成了数据集构建、训练、验证和导出，同时不会让 YOLO26 部分压过课程设计的板端人体识别计数主线。
