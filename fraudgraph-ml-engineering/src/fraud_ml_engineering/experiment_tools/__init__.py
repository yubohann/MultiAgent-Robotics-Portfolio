"""Operator entry points for experiment protocols, reports, and run manifests.

Each module is runnable with ``python -m fraud_ml_engineering.experiment_tools.<name>``;
the thin scripts under ``scripts/`` forward to the same ``main()`` functions.
"""

from __future__ import annotations
