# Local data directory

Place downloaded datasets and generated graph caches here. The default layout is documented in [`docs/data-and-reproduction.md`](../docs/data-and-reproduction.md).

The repository tracks code and documentation. Transaction records, benchmark archives, model checkpoints, and generated CSV outputs stay local.
