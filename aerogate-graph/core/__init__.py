"""Core algorithmic components of AeroGate Graph.

The paper is under review, so we are not releasing the implementation yet.
Each module in this directory keeps a short note on what the component does
and where it sits in the framework; the code ships once the paper is out.
"""