[project]
name = "aerogategraph"
version = "0.1.0"
description = "2D graph reinforcement learning for dense and dynamic drone gate traversal (single-agent and 8-drone formation)."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Bohan Yu" }]
dependencies = [
    "numpy>=1.24",
    "torch>=2.0",
]

[project.urls]
Homepage = "https://github.com/yubohann/MultiAgent-Robotics-Portfolio"
Repository = "https://github.com/yubohann/MultiAgent-Robotics-Portfolio"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.5",
]
viz = [
    "matplotlib>=3.8",
]

[project.scripts]
# No console scripts yet; entry points are per-line scripts under scripts/ and <line>/scripts/.

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Entry-point scripts under scripts/ bootstrap `sys.path` (or bind module globals at
# import time) before importing first-party packages, because they are executed
# directly from the repo instead of being installed. Module-level imports therefore
# intentionally appear below executable statements; E402 is expected in this pattern.
# In-package modules never get this exemption and keep all imports at the top.
"scripts/*.py" = ["E402"]
"**/scripts/*.py" = ["E402"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
