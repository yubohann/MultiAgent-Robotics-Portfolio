# 本地隔离声明

> 当前方法状态：`realised-QD + RL` 固定主链；OGFR 为链内改进；MIQD-compatible 为强基线；BCRR 失活。本文件只声明隔离与发布边界，不决定算法。

- 本目录暂不初始化 Git remote，不 push，不发布 package、checkpoint 或论文结果。
- AeroCityBench 不在本工作区的读取、转换、训练、适配或发布范围内；未经用户新的明确授权，任何公开 schema、train/validation、开发 evaluator、blind/private test 均不得访问。
- 若未来需要公开本目录，必须先完成许可证、第三方 NOTICE、数据泄漏、secret、checkpoint 和 blind-test transcript 审计，并由用户明确授权。
